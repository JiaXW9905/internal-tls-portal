# 产业互联网内部门户 高层设计文档（HLD）

## 1. 概览
本系统为企业内部服务统一门户，采用单体 Node.js Web 应用架构，提供多服务接入、统一认证、RBAC权限管理。当前首个接入服务为TLS证书管理，后续将接入更多企业服务。

**部署方式**：Docker 单容器 + SQLite + 本地文件存储（bind mount）

**当前版本**：v1.0.1

## 2. 目标与设计原则
- **统一入口**：所有企业服务通过统一门户访问，提供一致的用户体验
- **细粒度权限**：RBAC权限模型，支持服务级、功能级权限控制
- **可扩展性**：新服务接入机制清晰，改动成本低
- **向后兼容**：保持现有TLS服务功能和API完全兼容
- **最小复杂度**：单进程 + SQLite，便于内部快速交付
- **可升级/可回滚**：版本目录化部署（`internal-tls-portal-x.y.z`）
- **问题可排查**：明确健康检查、日志入口、数据目录和文件目录

## 3. 系统架构

### 3.1 整体架构
```
┌─────────────────────────────────────────┐
│          用户浏览器                      │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│        门户前端层                        │
│  - 门户首页 (portal-home.html)          │
│  - 服务导航 (sidebar-nav.js)            │
│  - TLS页面 (*.html)                     │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│       Express后端 (src/server.js)       │
│  ┌─────────────────────────────────┐    │
│  │  门户API (/api/portal/*)        │    │
│  │  - 服务列表                      │    │
│  │  - 权限查询                      │    │
│  │  - 角色管理                      │    │
│  └─────────────────────────────────┘    │
│  ┌─────────────────────────────────┐    │
│  │  TLS服务API (/api/*)            │    │
│  │  - 证书申请、签发、撤销          │    │
│  │  - 证书总览、导出                │    │
│  │  - 用户管理、系统设置            │    │
│  └─────────────────────────────────┘    │
│  ┌─────────────────────────────────┐    │
│  │  RBAC权限层                     │    │
│  │  - rbac-manager.js              │    │
│  │  - rbac-middleware.js           │    │
│  └─────────────────────────────────┘    │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│      SQLite数据库 (data/app.db)         │
│  - 用户表 (users)                       │
│  - TLS业务表 (requests, certificates)   │
│  - RBAC表 (8张表)                       │
│  - 审计日志表                           │
└─────────────────────────────────────────┘
```

### 3.2 技术栈
- **前端**：静态页面 + 原生 JavaScript（`public/*.html`, `public/*.js`）
- **后端**：Express 应用（`src/server.js`）
- **数据**：SQLite（`data/app.db`）
- **文件**：证书 ZIP 存储于 `uploads/`
- **会话**：`express-session` 内存存储（当前实现）
- **定时任务**：`node-cron`（过期提醒邮件）
- **权限管理**：自研RBAC模块（`rbac-manager.js`）

## 4. 运行时组件
- 应用入口：`src/server.js`
- 数据初始化与迁移：`src/db.js`
- 容器入口：`deploy/docker-entrypoint.sh`
- 编排：`docker-compose.yml`
- 环境变量：`deploy/env.prod`（基于 `deploy/env.example`）

## 5. 关键模块设计

### 5.1 鉴权与权限
- `requireAuth`：登录态校验。
- `requireRole`：基于角色的授权校验。
- `requireAdmin`：管理员专用接口。
- 数据可见范围在业务查询中进一步限制（如 `/api/requests`）。

### 5.2 请求与证书模块
- `requests` 表保存工单状态（`pending/issued/revoked/withdrawn`）。
- `request_certificates` 表保存证书文件与签发/撤销元数据。
- 查询时通过“每个 request 取最新 active cert”关联展示。

### 5.3 总览模块
- 入口接口：`GET /api/overview`、`GET /api/overview/export`
- 支持关键字/状态/即将过期筛选。
- 过期时间展示规则：优先显式字段，否则 `cert_created_at + 730 天`。

### 5.4 用户与设置模块
- 用户管理接口：角色调整、用户编辑、重置密码、邮箱验证。
- 系统设置：SMTP 与通知参数保存在 `system_settings`。

## 6. 数据流（简述）
1. 前端页面调用 `/api/*` 接口。
2. 后端完成会话鉴权和角色校验。
3. SQL 查询 SQLite 并按权限约束过滤。
4. 文件下载接口从 `uploads/` 读取并返回。
5. 关键操作写入数据库状态（签发/撤销/撤回等）。

## 7. 部署拓扑
- Docker 容器暴露 `52344` 端口。
- 宿主机目录映射：
  - `./data -> /app/data`
  - `./uploads -> /app/uploads`
- 优点：升级容器镜像时业务数据保持持久化。

## 8. 升级与回滚设计
- 升级：新版本目录解压 -> 复制历史数据目录与环境变量 -> 启动新容器。
- 回滚：停止当前版本 -> 启动上一版本目录容器（数据仍使用挂载目录）。
- 必做：升级前备份 `data/app.db` 和 `uploads/`。

## 9. 可观测性与排障入口
- 健康检查：`GET /api/health`
- 关键日志：容器日志（`docker logs <container>`）
- 常见问题：
  - 端口占用导致启动失败
  - SQL 语法问题导致 API 崩溃
  - 文件存在性与数据库记录不一致

## 10. RBAC权限系统设计（v1.0新增）

### 10.1 数据模型
```
portal_services (服务注册)
    ↓
permissions (权限定义：18个TLS权限)
    ↓
roles (角色定义：4个TLS角色)
    ↓
role_permissions (角色-权限映射)
    ↓
users ←→ user_roles (用户-角色映射)
    ↓
service_access_logs (访问审计)
permission_audit_logs (权限变更审计)
```

### 10.2 权限检查流程
1. 用户发起请求
2. 中间件检查用户是否登录
3. 查询用户在目标服务的角色
4. 查询角色拥有的权限
5. 判断是否有所需权限
6. 记录访问日志
7. 允许/拒绝请求

### 10.3 扩展性设计
- **服务隔离**：每个服务有独立的权限命名空间
- **权限继承**：支持全局角色和服务专属角色
- **临时授权**：支持权限过期时间
- **权限覆盖**：支持直接授权（允许/拒绝）

## 11. 当前技术债与改进建议
- `express-session` MemoryStore 不适合生产，建议迁移到 Redis
- SQLite 适合当前规模，后续并发增长时建议迁移 MySQL/PostgreSQL
- 建议补齐自动化测试（权限、分页、升级回归）
- 建议将部署脚本与发布流程标准化（一键预检 + 发布 + 验证 + 回滚）
- **新增**：考虑权限查询结果缓存，提升性能

## 12. 文档关联
- PRD：`docs/zh-CN/PRD.md`
- RBAC集成指南：`docs/zh-CN/RBAC-Integration-Guide.md`
- 实施计划：`docs/zh-CN/Portal-RBAC-Implementation-Plan.md`
- 后续文档：
  - 用户操作指南
  - 开发/运维 Runbook
  - 上线检查清单
  - LLM 快速上下文提示词

